spring.application.name=configurationserver
server.port=8888
spring.security.user.name=admin
spring.security.user.password=admin

# Serve configuration from the local classpath 'config/' directory for local development
# This uses files under src/main/resources/config/*.properties (e.g., authservice.properties)
spring.profiles.active=native

# Git backend settings (ignored when 'native' profile is active). Keep for future use.
# spring.cloud.config.server.git.uri=${CONFIG_REPO_URI:https://github.com/Hardik2345/Springbooot_microservices}
# spring.cloud.config.server.git.username=${CONFIG_GIT_USERNAME}
# spring.cloud.config.server.git.password=${CONFIG_GIT_PASSWORD}
# spring.cloud.config.server.git.search-paths=user-service,apigatway,post-service,servicediscovery,authservice
# spring.cloud.config.server.git.clone-on-start=true

